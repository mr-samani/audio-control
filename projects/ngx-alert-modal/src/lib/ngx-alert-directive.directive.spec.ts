import { NgxAlertDirectiveDirective } from './ngx-alert.directive';

describe('NgxAlertDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxAlertDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
